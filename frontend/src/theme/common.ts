export const customGeneralCardStyle = (colorMode) => {

    return {
        //bg: colorMode == "light" ? "blue.50" : "blue.700",
        w: "full",
        rounded: 20
    }

}

export const customBoldCardStyle = (colorMode) => {
    return {
       // bg: colorMode == "light" ? "blue.500" : "blue.700",
        w: "full",
        rounded: 20,
        //color: "white"
    }
}

export const customBoxMainContentStyle = (colorMode) => {
    return {
        mw: 700
    }
}